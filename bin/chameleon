#!/usr/bin/env node

const chameleon = require("../index");
const port      = process.argv[2];

if(!port || /^[0-9]+$/.test(port) !== true) {
  console.error("invalid port");
  process.exit(1);
}

chameleon.server.start(port);
